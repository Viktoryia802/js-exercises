function getNumber() {
  return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? Randomly generates a number and multiply it by 10
// - How many parameters does the function take?  None
// - What is the function name? getNumber
// - Where is the function called? with what parameters? the function is called in variable, no parameters
